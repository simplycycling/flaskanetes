apiVersion: v1
kind: Service
metadata:
  name: flaskanetes
  labels:
    app: flaskanetes
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: flaskanetes 